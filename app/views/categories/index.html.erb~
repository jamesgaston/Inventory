<% content_for :title do %>
  Categories
<% end %>


<h1>Categories</h1>

<% for c in @categories %>
	
	<%= link_to c.name, {:controller=>"categories", :action=>"edit", :id=>c.id},
	 {:title => "Edit Category"} %> (<%= c.id.to_s %>)<br/>


<% end %> 
<%= link_to "+New", new_category_url, {:title => "Add a new category"} %>